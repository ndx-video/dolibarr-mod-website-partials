# dolibarr-mod-website-partials
This Dolibarr module surfaces HTML partials from the CMS that uses the bundled Dolibarr website module. It has been designed for consumption by a web framework (like Astro) that needs to populate content "islands".
