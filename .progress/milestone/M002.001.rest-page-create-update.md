---
file: M002.001.rest-page-create-update.md
date: 2026-07-09
milestone: P2 — REST control plane
related_commits: uncommitted
---

# P2 — REST page create/update (no core Website API)

## Summary

Stock Dolibarr 22 has no Website REST API. Added `class/api_websitepartials.class.php` + `lib/websitepartials.lib.php` with health/list/get plus **POST/PUT** page endpoints. Created draft page `welcome` on website ref `testsite` via DOLAPIKEY on the local module-dev stack.

## Changes

- Implemented REST: `status`, `websites`, `websites/{ref}/pages`, `websites/{ref}/pages/{slug}`, POST create, PUT update
- Auth via `DOLAPIKEY`; authorization reuses Website module `read`/`write` rights
- Updated ROADMAP locked decision / P2 Done-when to allow create/update (still no DELETE)
- Updated module + root README / ChangeLog

## Decisions

- Class name `Websitepartials` (not `…Api`) so Restler mounts at `/websitepartials`
- Write API added despite earlier “read-only v1” note — required because core has no page CRUD API
- Public islands remain unpublished-safe: draft `status=0` will 404 on the future public path

## Follow-ups

- P1 public `.html`/`.json` router
- Confirm endpoints in API explorer UI
- Optional DELETE later if needed
- Do not commit API keys

## References

- Local verify: `GET/POST …/websitepartials/websites/testsite/pages` → `welcome` draft id=1
- [ROADMAP.md](../../ROADMAP.md) REST control plane
